# List literals (single and multi-line), index read/write, append/pop.
var xs = [1, 2, 3]
print(xs, len(xs))
xs[0] = 10
print(xs[0], xs[-1])
append(xs, 4)
print(xs)
print(pop(xs))
var ys = [
    "a",   # comment inside a literal
    "b",

    "c",
]
print(ys)
