use liphia_virtual_machine::vm::VM;

pub fn register(_vm: &mut VM) {
    // TODO: registrar builtins do core:
    //
    // len(x)
    // str(x)
    // int(x)
    // float(x)
    // time_now()
    //
    // Exemplo (depende da sua VM):
    // vm.register_native("len", native_len);
}